<div class="container">
  <h1 class="text-center m3">Booking info</h1>
  <% if current_user.venues.any? %>
    <%= render "shared/owner_booking_show" %>
  <% else %>
      <%= render "shared/customer_booking_show" %>
  <% end %>
</div>